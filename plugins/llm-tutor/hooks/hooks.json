{
  "description": "Two hooks: SessionStart refreshes statusline-integration symlinks so plugin upgrades don't strand them at old versions (no-op when the integration isn't installed). UserPromptSubmit runs the daily cycles refill check (no-op unless 24h have passed since the last reset).",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/refresh-symlinks.sh"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/refill-cycles.sh"
          }
        ]
      }
    ]
  }
}
